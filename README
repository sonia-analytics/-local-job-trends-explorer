#Sonia Mamnepuli
# Local Job Trends Explorer 

# A Python project analyzing local job market trends for Data Analyst positions in Philadelphia.

## Project Overview
- Scrapes job postings from LinkedIn
- Analyzes skill frequencies
- Visualizes trends
- Interactive dashboard

## Technologies Used
- Python
- Jupyter Notebook
- Requests + BeautifulSoup (scraping)
- Pandas (data manipulation)
- Matplotlib/Plotly (visualization)
- Streamlit (interactive dashboard)

## Setup
1. Clone this repository
2. Create virtual environment: `python -m venv env`
3. Activate environment: `source env/bin/activate` (Mac/Linux) or `env\Scripts\activate` (Windows)
4. Install dependencies: `pip install -r requirements.txt`
